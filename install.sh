cp .env.example .env && docker-compose up -d laravel.test && docker-compose exec laravel.test composer install && docker-compose down && ./vendor/bin/sail up -d && ./vendor/bin/sail artisan migrate:refresh && sleep 10 && ./vendor/bin/sail artisan db:seed  && ./vendor/bin/sail exec frontend cp .env-example .env && ./vendor/bin/sail exec frontend npm install && ./vendor/bin/sail exec frontend npm install && ./vendor/bin/sail exec frontend npm run serve
